version: 0.2

phases:
  build:
    commands:
      - ls
      - cd src/microservice.api.rest && dotnet restore
      - cd src/microservice.api.rest && dotnet build
artifacts:
  files:
    - src/microservice.api.rest/bin/Debug/netcoreapp2.0/*