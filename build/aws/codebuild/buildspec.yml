version: 0.2

phases:
  build:
    commands:
      - cd lambdarestapi.aws.lamda.host && dotnet restore
      - cd lambdarestapi.aws.lamda.host && dotnet build
artifacts:
  files:
    - lambdarestapi.aws.lamda.host/bin/Debug/netcoreapp2.0/*